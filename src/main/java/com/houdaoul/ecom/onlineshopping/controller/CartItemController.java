package com.houdaoul.ecom.onlineshopping.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/cartItems")
public class CartItemController {
/*
    private final CartItemService cartItemService;

    public CartItemController(CartItemService cartItemService) {
        this.cartItemService = cartItemService;
    }

    @PostMapping
    public ResponseEntity<UUID> createCartItem() {
        return ResponseEntity.ok().body(cartItemService.createCartItem(1L, 2));
    }

    @GetMapping("/{id}")
    public ResponseEntity<CartItem> getCartItem(@PathVariable UUID id) {
        return ResponseEntity.ok().body(cartItemService.getCartItem(id));
    }

 */
}
